# Инструкция по поиску структуры HTML для парсинга расписания

Эта инструкция поможет вам найти точную структуру HTML элементов, которые содержат информацию о расписании на сайте my.itmo.ru.

## 🎯 Цель

Найти структуру HTML элементов, которые содержат:
- Название предмета
- Время начала и конца пары
- Аудиторию
- Адрес здания
- Преподавателя

## 📋 Пошаговая инструкция

### Шаг 1: Откройте страницу расписания

1. Откройте браузер (Chrome, Firefox, Edge)
2. Войдите на https://my.itmo.ru
3. Перейдите на страницу расписания
4. Убедитесь, что видите расписание на экране

### Шаг 2: Откройте DevTools

**В Chrome/Edge:**
- Нажмите `F12` или `Ctrl+Shift+I` (Windows/Linux)
- Или `Cmd+Option+I` (Mac)
- Или правой кнопкой мыши на странице → "Просмотреть код" / "Inspect"

**В Firefox:**
- Нажмите `F12` или `Ctrl+Shift+I` (Windows/Linux)
- Или `Cmd+Option+I` (Mac)

### Шаг 3: Найдите контейнер расписания

1. В DevTools перейдите на вкладку **"Elements"** (Элементы) или **"Inspector"**
2. Нажмите на иконку **"Select element"** (обычно в левом верхнем углу, выглядит как курсор с квадратом)
3. Наведите курсор на **любой предмет** в расписании и кликните
4. В панели Elements вы увидите выделенный HTML элемент

### Шаг 4: Изучите структуру одного предмета

1. **Найдите родительский контейнер:**
   - Используя стрелки влево/вправо в панели Elements, найдите самый верхний элемент, который содержит ВСЮ информацию об одном предмете
   - Обычно это `<div>` с каким-то классом или data-атрибутом

2. **Изучите структуру внутри:**
   - Разверните этот элемент (кликните на стрелку слева)
   - Посмотрите, какие дочерние элементы содержат:
     - Название предмета
     - Время (начало и конец)
     - Аудиторию
     - Адрес
     - Преподавателя

3. **Обратите внимание на:**
   - **Классы** элементов (например: `class="lesson-title"`)
   - **Data-атрибуты** (например: `data-v-1ef6ff98`)
   - **Структуру вложенности** (какие элементы внутри каких)

### Шаг 5: Найдите селекторы для каждого поля

Для каждого элемента информации (предмет, время, аудитория и т.д.):

1. **Правой кнопкой мыши** на элементе в браузере
2. Выберите **"Inspect"** или **"Просмотреть код"**
3. В панели Elements элемент будет выделен
4. **Правой кнопкой мыши** на выделенном элементе в панели Elements
5. Выберите **"Copy"** → **"Copy selector"** или **"Copy XPath"**

**Примеры того, что нужно найти:**

```
Предмет (название):
- Селектор: div.lesson-title или div[data-v-xxx].title
- Как найти: текст с названием предмета

Время начала:
- Селектор: div.time-start или span.start-time
- Как найти: время начала пары (например, "09:00")

Время конца:
- Селектор: div.time-end или span.end-time
- Как найти: время конца пары (например, "10:30")

Аудитория:
- Селектор: div.room или span.audience
- Как найти: номер аудитории (например, "101" или "ауд. 101")

Адрес:
- Селектор: div.address или div.building
- Как найти: адрес здания (например, "Ломоносова, 9")

Преподаватель:
- Селектор: div.teacher или a.teacher-link
- Как найти: имя преподавателя
```

### Шаг 6: Проверьте разные дни недели

1. **Переключите расписание на другой день** (если есть такая возможность на странице)
2. Повторите шаги 3-5 для другого дня
3. Убедитесь, что структура HTML одинаковая для всех дней

### Шаг 7: Проверьте структуру контейнера всех предметов

1. Найдите элемент, который содержит **ВСЕ предметы** на странице
2. Обычно это:
   - `<div>` с классом типа `schedule-list`, `lessons-container`, или похожим
   - Или элемент, который вы нашли на шаге 3 (контейнер расписания)

3. **Проверьте структуру:**
   - Как предметы расположены относительно друг друга?
   - Есть ли обертка для каждого предмета?
   - Есть ли разделители между предметами?

### Шаг 8: Сохраните информацию

Запишите или скопируйте:

1. **Полный путь к контейнеру расписания:**
   ```
   div.layout-container
     → div.layout-content
       → div.router-transitions
         → div.row (data-v-4c581254)
           → div.col-12 (data-v-4c581254)
             → div.b-overlay-wrap
               → div (data-v-1ef6ff98) <- здесь предметы
   ```

2. **Структуру одного предмета:**
   ```
   div.lesson-item (или как он называется)
     → div.lesson-title (название)
     → div.lesson-time (время)
       → span.start (начало)
       → span.end (конец)
     → div.lesson-room (аудитория)
     → div.lesson-address (адрес)
     → div.lesson-teacher (преподаватель)
   ```

3. **Точные селекторы для каждого поля:**
   - Название: `div.lesson-title` или `h3.lesson-name`
   - Время: `div.time` или `span.schedule-time`
   - Аудитория: `div.room` или `span.auditorium`
   - Адрес: `div.address` или `div.building`
   - Преподаватель: `div.teacher` или `a.teacher-name`

## 🔍 Альтернативный способ: через Console

1. Откройте DevTools → вкладка **"Console"**
2. Введите команду для поиска элементов:

```javascript
// Найти все элементы с классом "lesson"
document.querySelectorAll('.lesson')

// Найти контейнер расписания
document.querySelector('.layout-container')

// Найти все предметы внутри контейнера
document.querySelector('[data-v-1ef6ff98]').querySelectorAll('div')
```

3. Изучите результаты в консоли

## 📝 Что нужно предоставить

После исследования предоставьте:

1. **Структуру контейнера расписания** (как на шаге 8.1)
2. **Структуру одного предмета** (как на шаге 8.2)
3. **Точные селекторы** для каждого поля (как на шаге 8.3)
4. **Пример HTML кода** одного предмета (скопируйте из Elements)

## 💡 Полезные советы

- Используйте **"Copy" → "Copy outerHTML"** для копирования полного HTML элемента
- Используйте **"Copy" → "Copy selector"** для получения CSS селектора
- Если элементы генерируются через JavaScript, они могут не быть видны в исходном HTML - в этом случае нужно искать данные в JavaScript переменных
- Проверьте Network tab - возможно, расписание загружается через API запрос

## 🚨 Если структура не найдена

Если не можете найти структуру:

1. **Проверьте, загружается ли расписание через JavaScript:**
   - Откройте Network tab
   - Обновите страницу
   - Найдите запросы к API (обычно `/api/schedule` или похожее)
   - Если есть - скопируйте URL и параметры запроса

2. **Проверьте JavaScript переменные:**
   - В Console введите: `window` и нажмите Enter
   - Ищите переменные типа `__INITIAL_STATE__`, `schedule`, `lessons` и т.д.

3. **Сообщите разработчику:**
   - Пришлите скриншот страницы расписания
   - Пришлите HTML код контейнера расписания (Copy outerHTML)
   - Пришлите информацию о том, загружается ли расписание через API
